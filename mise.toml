[tools]
bun = "1.3.10"

[tasks.setup]
description = "Install project dependencies"
run = "bun install"

[tasks.check]
description = "Run format, lint, and Astro checks"
run = "bun run check"

[tasks.build]
description = "Build static site"
run = "bun run build"
