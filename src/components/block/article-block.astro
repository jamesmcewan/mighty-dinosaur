---
import Category from "@/components/inline/category-inline.astro"
const { title, category, slug } = Astro.props

// Split title and category into words for view transitions
const titleWords = title ? title.split(" ") : []
const categoryWords = category ? category.split(" ") : []
---

<article
  class="prose-lg prose font-rethink text-fw-text prose-headings:font-cartridgerough prose-headings:uppercase prose-headings:text-fw-text prose-h1:font-cartridgeboldrough prose-h1:text-4xl prose-h1:text-fw-ember prose-h2:text-fw-clay prose-h3:text-fw-umber prose-strong:text-fw-sienna prose-a:text-fw-sienna/85 prose-a:hover:text-fw-clay prose-img:rounded-xl hover:brightness-110"
>
  <header>
    {category && (
      <Category>
        {categoryWords.map((word, index) => (
          <span
            style={{
              viewTransitionName: slug ? `category-${slug}-word-${index}` : undefined,
            }}
          >
            {word}{index < categoryWords.length - 1 ? " " : ""}
          </span>
        ))}
      </Category>
    )}
    {title && (
      <h1>
        {titleWords.map((word, index) => (
          <span
            style={{
              viewTransitionName: slug ? `title-${slug}-word-${index}` : undefined,
            }}
          >
            {word}{index < titleWords.length - 1 ? " " : ""}
          </span>
        ))}
      </h1>
    )}
  </header>
    <div data-content="post">
      <slot />
    </div>
</article>
